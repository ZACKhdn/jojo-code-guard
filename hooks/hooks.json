{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash --norc --noprofile -c 'script=\"${CLAUDE_PLUGIN_ROOT//\\\\\\\\//}/hooks/session-start\"; exec bash --norc --noprofile \"$script\"'",
            "shell": "bash",
            "async": false
          }
        ]
      }
    ]
  }
}
